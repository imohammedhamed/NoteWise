import MaxWContainer from "../ui/MaxWContainer"
export default function UserWorkingSpaceFolderBanner() {
  return (
    <div className="w-full flex justify-center items-center p-2 bg-Purple300">
        <p className=" lg:text-sm text-xs text-center font-extrabold text-DarkPurple">{`✨ Now, you can organize multiple tables in a single workspace 'folder'.`}</p>
    </div>
  )
}
