import MaxWContainer from "../ui/MaxWContainer"
export default function UserWorkingSpaceFolderBanner() {
  return (
    <div className="w-full flex justify-center items-center p-2 bg-brand_fourthary/90">
        <p className=" lg:text-sm text-xs text-center font-extrabold text-brand_tertiary">{`✨ Now, you can organize multiple tables in a single workspace 'folder'.`}</p>
    </div>
  )
}
